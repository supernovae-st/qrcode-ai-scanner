--- crates/qrcode-ai-scanner/src/score/uec.rs
+++ replace == with != in zigzag_positions
@@ -195,17 +195,17 @@
         if !reserved_cell(version, y, x - 1) {
             positions.push((y, x - 1));
         }
         let (new_y, new_neg_dir) = match (y, neg_dir) {
             (0, true) => {
                 x = x.saturating_sub(2);
                 (0, false)
             }
-            (yy, false) if yy == size - 1 => {
+            (yy, false) if yy != /* ~ changed by cargo-mutants ~ */ size - 1 => {
                 x = x.saturating_sub(2);
                 (size - 1, true)
             }
             (yy, true) => (yy - 1, true),
             (yy, false) => (yy + 1, false),
         };
         y = new_y;
         neg_dir = new_neg_dir;
