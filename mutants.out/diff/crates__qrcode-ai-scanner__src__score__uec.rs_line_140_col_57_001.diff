--- crates/qrcode-ai-scanner/src/score/uec.rs
+++ replace + with * in reserved_cell
@@ -132,17 +132,17 @@
     if i < 9 && j + 8 >= size {
         return true;
     }
     // timing patterns
     if i == 6 || j == 6 {
         return true;
     }
     // version info (v7+)
-    if version >= 7 && ((i < 6 && j + 11 >= size) || (i + 11 >= size && j < 6)) {
+    if version >= 7 && ((i < 6 && j + 11 >= size) || (i * /* ~ changed by cargo-mutants ~ */ 11 >= size && j < 6)) {
         return true;
     }
     // alignment patterns
     let mut ai = None;
     let mut aj = None;
     let mut len = 0;
     for (a, &pattern) in ver.apat.iter().take_while(|&&p| p != 0).enumerate() {
         len = a;
