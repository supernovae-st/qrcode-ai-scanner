--- crates/qrcode-ai-scanner/src/score/uec.rs
+++ replace != with == in reserved_cell
@@ -139,17 +139,17 @@
     // version info (v7+)
     if version >= 7 && ((i < 6 && j + 11 >= size) || (i + 11 >= size && j < 6)) {
         return true;
     }
     // alignment patterns
     let mut ai = None;
     let mut aj = None;
     let mut len = 0;
-    for (a, &pattern) in ver.apat.iter().take_while(|&&p| p != 0).enumerate() {
+    for (a, &pattern) in ver.apat.iter().take_while(|&&p| p == /* ~ changed by cargo-mutants ~ */ 0).enumerate() {
         len = a;
         if pattern.abs_diff(i) < 3 {
             ai = Some(a);
         }
         if pattern.abs_diff(j) < 3 {
             aj = Some(a);
         }
     }
