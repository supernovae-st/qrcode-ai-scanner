--- crates/qrcode-ai-scanner/src/score/uec.rs
+++ replace < with == in reserved_cell
@@ -144,17 +144,17 @@
     let mut ai = None;
     let mut aj = None;
     let mut len = 0;
     for (a, &pattern) in ver.apat.iter().take_while(|&&p| p != 0).enumerate() {
         len = a;
         if pattern.abs_diff(i) < 3 {
             ai = Some(a);
         }
-        if pattern.abs_diff(j) < 3 {
+        if pattern.abs_diff(j) == /* ~ changed by cargo-mutants ~ */ 3 {
             aj = Some(a);
         }
     }
     match (ai, aj) {
         (Some(x), Some(y)) if x == len && y == len => true,
         (Some(x), Some(_)) if 0 < x && x < len => true,
         (Some(_), Some(y)) if 0 < y && y < len => true,
         _ => false,
