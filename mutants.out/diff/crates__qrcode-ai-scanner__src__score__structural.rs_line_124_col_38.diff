--- crates/qrcode-ai-scanner/src/score/structural.rs
+++ replace / with % in quiet_zone_ok
@@ -116,17 +116,17 @@
             }
             mx += step;
         }
         my += step;
     }
     if interior_n == 0 {
         return false;
     }
-    let interior_mean = interior_sum / interior_n;
+    let interior_mean = interior_sum % /* ~ changed by cargo-mutants ~ */ interior_n;
 
     // probe a 2-module ring outside each edge
     let mut light = 0u32;
     let mut total = 0u32;
     for offset in [-2i32, -1] {
         for m in -2..=(modules + 1) {
             for (mx, my) in [
                 (m, offset),               // top
